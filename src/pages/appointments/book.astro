---
import Layout from "../../layouts/Layout.astro";
import AppointmentCalendar from "../../components/calendar/AppointmentCalendar";
---

<Layout
  title="Reservar Cita"
  description="Reserva una nueva cita seleccionando fecha, hora y proveedor."
>
  <main class="min-h-screen py-12">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h1 class="text-3xl font-extrabold text-gray-900 sm:text-4xl">
          Reservar Nueva Cita
        </h1>
        <p class="mt-3 max-w-2xl mx-auto text-xl text-gray-500 sm:mt-4">
          Selecciona proveedor, fecha y hora para tu cita
        </p>
      </div>

      <div class="max-w-xl mx-auto bg-white shadow rounded-lg overflow-hidden">
        <div class="p-6">
          <AppointmentCalendar client:load />
        </div>
      </div>
    </div>
  </main>
</Layout>

<script>
  // Script to handle appointment selection
  document.addEventListener('DOMContentLoaded', function() {
    console.log('Appointment booking page loaded');
  });
</script>